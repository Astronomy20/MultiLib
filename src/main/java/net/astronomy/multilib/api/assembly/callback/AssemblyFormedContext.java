package net.astronomy.multilib.api.assembly.callback;

/** Passed to {@link AssemblyFormedCallback} when an assembly is fully formed. */
public record AssemblyFormedContext(AssemblyContext context) {}
